apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: cnpg-validation-daily
  namespace: postgresql
spec:
  schedule: "0 0 2 * * *"
  backupOwnerReference: self
  cluster:
    name: cnpg-validation
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
