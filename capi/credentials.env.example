# Copy this file to credentials.env, replace the placeholders, and run:
#   chmod 600 capi/credentials.env
#
# Keep values single-quoted so shell metacharacters are not interpreted.
PROXMOX_URL='https://proxmox.example:8006'
PROXMOX_TOKEN='capmox@pve!capi'
PROXMOX_SECRET='replace-me'
